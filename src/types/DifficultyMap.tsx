import type { Difficulty } from "./Difficulty";
import type { DifficultyLabel } from "./DifficultyLabel";

export const DifficultyMap: Record<Difficulty, DifficultyLabel> = {
    easy: 'Easy',
    medium: 'Medium',
    hard: 'Hard',
};
